import 'package:flutter/material.dart';

class SearchTransactionHistory extends StatelessWidget {
  const SearchTransactionHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
